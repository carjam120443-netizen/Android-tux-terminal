# pkg search

Searches package display names, package IDs, descriptions, and aliases.

Examples:

```text
pkg search firefox
pkg search fdroid
pkg search github
pkg install firefox
```

Users do not need to know an Android package ID such as `com.example.app`. The catalog can provide a friendly `name`, optional `package`, and `aliases`.
